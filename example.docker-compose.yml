version: '3'
services:
  main:
    tty: true
    volumes:
      - ./OpenUpgrade:/OpenUpgrade
      - ./in:/in
      - ./out:/out
      - ./inner-scripts:/scripts
    build:
      context: .
    image: odoo-upgrader
