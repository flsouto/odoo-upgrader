version: '3'
services:
  main:
    extends:
      file: ../docker-compose.yml
      service: main
    volumes:
      - ./inner-scripts:/upgrader-scripts
    build:
      context: .
    image: odoo-upgrader11
